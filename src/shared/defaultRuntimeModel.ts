/**
 * Default Ollama model tag for new sessions and when the UI needs a small, fast baseline.
 * Users can change it anytime; `ollama pull tinyllama` is enough to satisfy Start.
 */
export const DEFAULT_OLLAMA_MODEL_TAG = 'tinyllama'
